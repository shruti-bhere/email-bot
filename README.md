# [Email Bot]

> **Note:** Drafting email using LangGraph workflow. This project is *hello world program* for LangGraph

## 📋 Prerequisites

Before you begin, ensure you have met the following requirements:
* **Python 3.8+** installed on your machine.
* [Optional: Any specific API keys or environment variables needed].

## ⚙️ Installation

1.  **Clone the repository:**
    ```bash
    git clone https://github.com/shruti-bhere/email-bot.git
    ```

2.  **Navigate to the project directory:**
    ```bash
    cd email-bot
    ```

3.  **(Optional but recommended) Create and activate a virtual environment:**
    ```bash
    # Windows
    python -m venv venv
    venv\Scripts\activate

    # macOS/Linux
    python3 -m venv venv
    source venv/bin/activate
    ```

4.  **Install dependencies:**
    ```bash
    pip install -r requirements.txt
    ```

## 🚀 Usage

To run the agent, navigate to the source directory and execute the main script:

```bash
cd agent
python main.py